Ты работаешь с ТЕКУЩИМ состоянием репозитория. Я уже редактировал проект, поэтому не опирайся на старые версии, старые промты и внешние описания, если они не подтверждаются текущими файлами.

Твоя задача:
обновить тексты страницы /artist, используя только готовый source-of-truth файл с текстами. Не придумывай новый текст, не переписывай стиль и не заменяй формулировки синонимами.

SOURCE OF TRUTH:
используй файл `09_SOURCE_JSON/artist_page_copy_for_codex.json` как единственный источник текстов.

Работай так:
1. Найди файлы, которые реально рендерят страницу /artist.
2. Найди, где хранятся тексты: компонент, data-файл, json, markdown, i18n, cms-конфиг.
3. Сопоставь текущие блоки страницы с ключами из JSON:
   - heroSubtitle
   - intro
   - path
   - familyTitle
   - familyText
   - geography
   - heritage
4. Если на странице уже есть фактографическая врезка, таймлайн, chips или маршрутные карточки, используй:
   - factBox
   - periods
5. Если таких блоков нет, не создавай большие новые секции без необходимости.
6. Вноси минимальные изменения и не ломай существующую архитектуру.

ВАЖНО:
- не сочиняй текст;
- не сокращай и не расширяй тексты;
- не меняй формулировки;
- не перестраивай страницу радикально;
- не меняй стили, классы, маршруты, ключи локализации, названия компонентов и пропсов без крайней необходимости;
- меняй прежде всего данные/строки, а не разметку.

Порядок смысловых блоков на странице:
1. hero subtitle
2. intro
3. path / biography
4. family
5. geography
6. heritage

Дополнительные редакционные ограничения:
- семейный блок должен работать как среда формирования художника;
- не смещать акцент на выставочную схему «два поколения»;
- географический блок должен показывать широкий маршрут, а не только Русский Север;
- финальный блок не должен начинаться с технических формул о цифровом архиве;
- если на странице уже есть таймлайн или карточки маршрутов, лучше использовать periods и factBox именно там.

После правок:
- посмотри package.json;
- запусти только реально существующие команды проверки;
- если доступны lint / typecheck / build, выполни их;
- не исправляй посторонние части проекта.

Финальный отчёт:
1. перечисли изменённые файлы;
2. укажи, в какие именно блоки страницы были подставлены тексты;
3. отдельно напиши, использовал ли ты factBox и periods;
4. если какой-то блок отсутствовал, укажи, куда именно встроил текст вместо него.
